version: '3.8'

services:
  llm-api:
    build: ./llm-api
    ports:
      - "8080:8080"

  firewall:
    build: ./firewall
    ports:
      - "8000:8000"
    depends_on:
      - llm-api

  dashboard:
    build: ./dashboard
    ports:
      - "3000:3000"
    depends_on:
      - firewall
